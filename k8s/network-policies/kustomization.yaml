apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: network-policies

resources:
  - default-deny.yaml
  - production-policies.yaml
  - develop-policies.yaml
  - test-policies.yaml

commonLabels:
  app.kubernetes.io/managed-by: kustomize
  app.kubernetes.io/part-of: lims-security
