# Grafana Dashboard Provisioning
apiVersion: 1

providers:
  - name: 'LIMS Dashboards'
    orgId: 1
    folder: 'LIMS'
    folderUid: 'lims'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
